# Scripts
Various small scripts and handy utilities.

## Documentation
### rename.py
This script renames files in a specified folder by adding a prefix based on the folder name and numbering them sequentially.
Run it from the parent folder and pass the name of the target folder to the script (escape spaces with \):
`python3 rename.py My\ Photos`
